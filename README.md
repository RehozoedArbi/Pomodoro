
  # AuraFocus Pomodoro Timer SPA

  This is a code bundle for AuraFocus Pomodoro Timer SPA. The original project is available at https://www.figma.com/design/zbYQ8U9hA7SyEzJF15ofIj/AuraFocus-Pomodoro-Timer-SPA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  